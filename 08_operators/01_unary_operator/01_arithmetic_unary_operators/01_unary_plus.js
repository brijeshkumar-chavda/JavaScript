// Topic: Unary `+`` Operator

// Q. What is `+` Operator?
// -> It is nothing but `+` symbol which use to convert number which written in string format to number.

// Example
const numberAsString = "123";
const convertToNumber = +numberAsString;
console.log(typeof convertToNumber);
