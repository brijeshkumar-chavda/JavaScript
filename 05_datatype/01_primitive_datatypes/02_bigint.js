// Topic: BigInt

// Note:
// - BigInt numbers can only perform operation with BigInt datatype

// Syntax:
// Creating BigInt number using BigInt() constructor
let bigInt1 = BigInt(1000);

// Creating BigInt number using `n` at the end of the number
let bigInt2 = 1000n;
