// Topic: Date

// Note:
// -> In JavaScript month start from 0 (Jan) to 11 (Dec).

// Syntax:
let date = new Date();
console.log(date); 

// Different formate of date output 
console.log(date.toString()); 
console.log(date.toISOString()); 
console.log(date.toJSON()); 
